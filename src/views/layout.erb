<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="shortcut icon" href="/assets/favicon.ico" type="image/x-icon" />
  <link href="/assets/bootstrap-2.0.0/css/bootstrap.css" rel="stylesheet">
  <link href="/assets/bootstrap-2.0.0/css/bootstrap-responsive.css" rel="stylesheet">
  <style>
    body {
      padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
    }
    .navbar-search .input-prepend {
      margin-bottom: 0;
    }
  </style>
</head>
<body data-spy="scroll" data-target=".subnav" data-offset="50">
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="brand" href="/">Giftudi</a>
        <ul class="nav">
          <li class="">
            <a href="/">Home</a>
          </li>
        </ul>

        <% unless signed_in? then %>
        <form action="/login" method="post" class="navbar-form pull-right">
          <input type="email"    class="input-small" placeholder="Email" name="email"/>
          <input type="password" class="input-small" placeholder="Password" name="password"/>
          <button type="submit"  class="btn">Sign In</button>
          <%= Rack::Csrf.tag(env) %>
        </form>
        <% else %>
        <%= user.email %>
        <form action="/logout" method="post" class="navbar-form pull-right">
          <button type="submit" class="btn">Sign Out</button>
          <%= Rack::Csrf.tag(env) %>
        </form>
        <% end %>
                  

      </div>
    </div>
  </div>
  <div class="container">
    <%= yield %>
  </div>
  <script src="/assets/bootstrap-2.0.0/js/bootstrap.js"></script>
</body>